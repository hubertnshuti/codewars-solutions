<h2><a href=https://www.codewars.com/kata/5701e43f86306a615c001868/train/typescript target="_blank">Credit card issuer checking</a></h2><h3>7 kyu</h3><p>Given a credit card number we can determine who the issuer/vendor is with a few basic knowns.</p><p>Complete the function <code>getIssuer()</code> that will use the values shown below to determine the card issuer for a given card number. If the number cannot be matched then the function should return the string <code>Unknown</code>.</p><p>Where <code>Issuer</code> is defined with the following enum type.</p><pre><code class="language-typescript"><span class="cm-keyword">enum</span> <span class="cm-def">Issuer</span> {  <span class="cm-def">VISA</span> <span class="cm-operator">=</span> <span class="cm-string">'VISA'</span>,  <span class="cm-def">AMEX</span> <span class="cm-operator">=</span> <span class="cm-string">'AMEX'</span>,  <span class="cm-def">Mastercard</span> <span class="cm-operator">=</span> <span class="cm-string">'Mastercard'</span>,  <span class="cm-def">Discover</span> <span class="cm-operator">=</span> <span class="cm-string">'Discover'</span>,  <span class="cm-def">Unknown</span> <span class="cm-operator">=</span> <span class="cm-string">'Unknown'</span>,}</code></pre><pre style="display: none;"><code class="language-markdown">| Card Type  | Begins With          | Number Length ||------------|----------------------|---------------|| AMEX       | 34 or 37             | 15            || Discover   | 6011                 | 16            || Mastercard | 51, 52, 53, 54 or 55 | 16            || VISA       | 4                    | 13 or 16      |</code></pre><h2 id="examples">Examples</h2><pre style="display: none;"><code class="language-js">getIssuer(4111111111111111) == "VISA"getIssuer(4111111111111) == "VISA"getIssuer(4012888888881881) == "VISA"getIssuer(378282246310005) == "AMEX"getIssuer(6011111111111117) == "Discover"getIssuer(5105105105105100) == "Mastercard"getIssuer(5105105105105106) == "Mastercard"getIssuer(9111111111111111) == "Unknown"</code></pre><pre style="display: none;"><code class="language-nasm"><span class="cm-keyword">mov</span> <span class="cm-builtin">rdi</span>, <span class="cm-number">4111111111111111</span><span class="cm-keyword">call</span> getissuer      <span class="cm-comment">; RAX &lt;- "VISA"</span>                    <span class="cm-keyword">mov</span> <span class="cm-builtin">rdi</span>, <span class="cm-number">4111111111111</span><span class="cm-keyword">call</span> getissuer      <span class="cm-comment">; RAX &lt;- "VISA"</span><span class="cm-keyword">mov</span> <span class="cm-builtin">rdi</span>, <span class="cm-number">4012888888881881</span><span class="cm-keyword">call</span> getissuer      <span class="cm-comment">; RAX &lt;- "VISA"</span><span class="cm-keyword">mov</span> <span class="cm-builtin">rdi</span>, <span class="cm-number">378282246310005</span><span class="cm-keyword">call</span> getissuer      <span class="cm-comment">; RAX &lt;- "AMEX"</span><span class="cm-keyword">mov</span> <span class="cm-builtin">rdi</span>, <span class="cm-number">6011111111111117</span><span class="cm-keyword">call</span> getissuer      <span class="cm-comment">; RAX &lt;- "Discover"</span><span class="cm-keyword">mov</span> <span class="cm-builtin">rdi</span>, <span class="cm-number">5105105105105100</span><span class="cm-keyword">call</span> getissuer      <span class="cm-comment">; RAX &lt;- "Mastercard"</span><span class="cm-keyword">mov</span> <span class="cm-builtin">rdi</span>, <span class="cm-number">5105105105105106</span><span class="cm-keyword">call</span> getissuer      <span class="cm-comment">; RAX &lt;- "Mastercard"</span><span class="cm-keyword">mov</span> <span class="cm-builtin">rdi</span>, <span class="cm-number">9111111111111111</span><span class="cm-keyword">call</span> getissuer      <span class="cm-comment">; RAX &lt;- "Unknown"</span></code></pre>