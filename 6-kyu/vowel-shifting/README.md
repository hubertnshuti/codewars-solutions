<h2><a href=https://www.codewars.com/kata/577e277c9fb2a5511c00001d/train/javascript target="_blank">Vowel Shifting</a></h2><h3>6 kyu</h3><p>You get a <code>text</code> and have to shift the vowels by <code>n</code> positions to the right. (Negative value for n should shift to the left.)</p><p><code>Position</code> means the vowel's position if taken as one item in a list of all vowels within the string.</p><p>A shift by 1 would mean, that every vowel shifts to the place of the next vowel.</p><p>Shifting over the edges of the text should continue at the other edge.</p><p>Vowels are "aeiou" / "AEIOU".</p><p>If text is null or empty, then simply return back the original text.</p><h3 id="example">Example:</h3><pre><code class="language-python"><span class="cm-variable">text</span> <span class="cm-operator">=</span> <span class="cm-string">"This is a test!"</span><span class="cm-variable">n</span> <span class="cm-operator">=</span> <span class="cm-number">1</span><span class="cm-variable">output</span> <span class="cm-operator">=</span> <span class="cm-string">"Thes is i tast!"</span><span class="cm-variable">text</span> <span class="cm-operator">=</span> <span class="cm-string">"This is a test!"</span><span class="cm-variable">n</span> <span class="cm-operator">=</span> <span class="cm-number">3</span><span class="cm-variable">output</span> <span class="cm-operator">=</span> <span class="cm-string">"This as e tist!"</span></code></pre><hr><p>Have fun coding it and please don't forget to vote and rank this kata! :-)</p><p>I have created other katas. Have a look if you like coding and challenges.</p>